[project]
name = "hqq-integrations-tests"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "hqq>=0.2.1",
    "pytest>=9.0.1",
    "tinyquant",
]

[tool.uv.sources]
tinyquant = { workspace = true }
